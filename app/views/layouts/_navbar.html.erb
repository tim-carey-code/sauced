<% if turbo_native_app? %>
  
<% else %>

  <nav class="navbar navbar-dark navbar-expand-lg bg-dark bg-flames bg-75">
    <div class="container-fluid">
      <%= link_to "You've Been Sauced", root_path, class: "navbar-brand fw-semibold" %>

      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>

      <div class="collapse navbar-collapse" id="navbarSupportedContent">
        
        <ul class="navbar-nav me-auto mb-2 mb-lg-0">
          <li class="nav-item">
            <%= link_to "Sauces", hotsauces_path, class: "nav-link text-light" %>
          </li>
          <li>
            <% if user_signed_in? %>
              <%= link_to "My Profile", user_profile_path(current_user), class: "nav-link text-light" %>
            <% end %>
          </li>
        </ul>

        <ul class="navbar-nav mb-2 mb-lg-0">
          <% if user_signed_in? %>

            <li class="nav-item dropdown m-0">
              <a class="nav-link dropdown-toggle text-light" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                <%= current_user.username %>
              </a>

              <ul class="dropdown-menu">
                <li>
                  <%= link_to "Edit Profile", edit_user_registration_path, class: "dropdown-item" %>
                </li>
                <li>
                  <%= button_to "Sign Out", destroy_user_session_path, data: { turbo: false }, method: :delete, class: "btn-link link-secondary dropdown-item" %>
                </li>
              </ul>
            </li>

          <% else %>

            <li class="nav-item">
              <%= link_to "Sign In", new_user_session_path, class: "nav-link btn btn-primary" %>
            </li>

          <% end %>
        </ul>


      </div>
    </div>
  </nav>

<% end %>
