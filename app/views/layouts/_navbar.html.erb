<nav style="background-color:white" class="navbar navbar-expand-lg bg-light">
  <div class="container-fluid">
    <%= link_to "You've Been Sauced", root_path, class: "navbar-brand fw-semibold" %>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
      </ul>
      <% if user_signed_in? && current_user.avatar.attached? %>
        <li class="nav-link"><%= current_user.username %></li>
        <ul class="nav-item dropdown m-0">
          <a class="nav-link" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
            <%= image_tag current_user.avatar.variant(:thumb), class: "user-avatar mb-2 border border-dark rounded-circle me-2" %>
          </a>
          <ul class="dropdown-menu">
            <li><%= link_to "Sign Out", destroy_user_session_path, data: { turbo_method: :delete }, class: "dropdown-item" %></li>
          </ul>
        </ul>
      <% elsif !user_signed_in? %>
        <%= link_to "Sign In", new_user_session_path, class: "btn btn-primary" %>
      <% end %>
    </div>
  </div>
</nav>
